# R10 — State the measured threshold in `persian-style.md`

status: done
date: 2026-08-20
branch: governance-ci-four-gates
commit: d7c1dc8
outstanding: none

## What changed

`docs/agents/persian-style.md` now states the threshold R9 measured instead of
an absolute ban, and says plainly where the gate is stricter than the rendering.
A reader of rule 2 can now predict, on a concrete string, whether the suite goes
red — including the three cases where the gate fails a string that renders
correctly.

## Files

- `docs/agents/persian-style.md` — rule 2 carries the measured threshold; the
  enforcement paragraph states the gate's real (over-strict) scope and narrows
  the fixture claim to five of six examples; the "Verifying a string" lead-in
  no longer implies the gate covers the whole trailing half.

## Line identities found

The plan's line numbers were written on 2026-08-18. Each was verified by content
before editing and each was still true:

- line 43 — rule 2, ending *"End-of-string does not save it."*
- line 58 — *"Rule 2 is enforced. … The worked examples above are the gate's own
  fixture …"*
- line 71 — *"The gate covers rule 2's trailing half. …"*

## Evidence

### Red observed

No behaviour change — no red required or available. This unit edits one Markdown
file; the bidi gate reads `src/i18n/locales/fa.json`, never this doc, so nothing
in the suite can respond to the change. What stands in its place is the
predict-then-run demonstration under `### Verification`: the doc's new claims are
executed against the gate's own helpers, sliced out of `src/meta/bidi.test.js` at
run time rather than reimplemented, so the doc is checked against the code it
describes instead of against a paraphrase of it.

`src/meta/bidi.test.js` was read but not modified — it is outside this unit's
`Files`. `git diff --stat` showed one file, 5 insertions, 3 deletions.

### Acceptance criteria

- [x] **Rule 2 states a threshold R9 measured** — checked by reading R9's
  `## What this decides` against the new rule 2. R9's rule: *"a trailing neutral
  run mirrors visibly when, after removing whitespace, two or more characters
  remain and they are not all the same character"*. The doc now says: *"a
  trailing neutral stretch mirrors visibly when, whitespace removed, two or more
  characters remain **and they are not all the same character**"* — the same
  rule, and every example it cites (`پشتیبانی VLESS://`, `متن VLESS:/ ادامه`,
  `از VLESS.`, `متن VLESS.مهم است`, `متن VLESS.. ادامه`) appears in R9's
  measurement tables with the verdict the doc gives it. The visual-order strings
  quoted for the two mirrored cases (`//:VLESS ینابیتشپ`, `همادا /:VLESS نتم`)
  are copied from R9's controls table, not retyped from reasoning.
  R9's "must not assert" is honoured: the doc does not claim the gate fails
  exactly the strings that render wrongly. It says the opposite, naming
  `VLESS. ` and `VLESS.. ` as failures that render correctly.
  The false claim in the gate's own comment at `bidi.test.js:70-76` — *"a
  sentence period plus its space lands on the far side of the Latin token it was
  meant to follow"* — is not repeated anywhere in the doc; the new text was
  searched for it and for the phrase "far side".
- [x] **the doc's coverage claim matches the gate's actual scope** — checked by
  reading `brokenRuns`/`latinRuns` (`src/meta/bidi.test.js:85-110`) against the
  two amended sentences.
  Code: runs are `latinRuns(value).filter(({ run }) => LETTER.test(run))`, then
  `.filter(({ trailing }) => trailing.length >= MIRRORS_VISIBLY)` with
  `MIRRORS_VISIBLY = 2`, and `isNeutral` counts any `\s` as neutral alongside
  `TOKEN_NEUTRAL`.
  Doc: *"fails the suite when a `fa.json` value ends a Latin run — a run carrying
  at least one `A-Za-z` — on two or more trailing neutrals … It counts
  whitespace and repeats toward those two"*. Each clause maps to one line of the
  code above.
  The fixture claim: `WORKED_EXAMPLES` (`src/meta/bidi.test.js:130-138`) holds
  seven rows — the doc's six worked examples plus `حداکثر 253. مقدار`, which the
  doc does not carry. Of the six, `دانلود .txt` is `broken: false` in the fixture
  while the doc's table measures it broken, carried with
  `why: 'measured broken, but by the LEADING neutral this gate does not judge'`.
  The doc now says exactly that: five of six are the fixture's verdicts, and the
  sixth is in the fixture as **not** broken with its reason attached.

### Verification

*A reader of rule 2 can predict what the gate will and will not fail.* —
confirmed by predicting first and running second. Six strings were predicted
from the amended doc text alone (rule 2 plus the enforcement paragraph), then
scored by the gate's own `brokenRuns`, extracted from `src/meta/bidi.test.js` at
run time:

```js
const body = src.slice(src.indexOf('const LATIN'), src.indexOf('const controlsIn'))
const brokenRuns = new Function(body + '\nreturn brokenRuns')()
```

```
MATCH  predicted=FAIL   actual=FAIL   ["VLESS://"]   پشتیبانی VLESS://
MATCH  predicted=clean  actual=clean  []             از VLESS.
MATCH  predicted=FAIL   actual=FAIL   ["VLESS. "]    یا رمزنگاری VLESS. هیچ
MATCH  predicted=FAIL   actual=FAIL   ["VLESS.. "]   متن VLESS.. ادامه
MATCH  predicted=clean  actual=clean  []             لیست IP/دامین CDN
MATCH  predicted=clean  actual=clean  []             حداکثر 253. مقدار
```

Six of six. The two that matter are rows 3 and 4: under the *old* rule 2 a
reader would have predicted `از VLESS.` fails ("End-of-string does not save it")
and would have had no way to predict that `متن VLESS.. ادامه` fails while
rendering correctly. Under the new text both are predictable — the first from
the threshold, the second from the sentence saying the gate counts whitespace
and repeats.

The predict script was scratch, lived in the session scratchpad, and is removed;
`git status --porcelain` shows only this record and the doc.

### Tests

scope: full

`npm run test` → `Test Files 9 passed (9)`, `Tests 1011 passed (1011)` —
baseline held exactly, including `src/meta/bidi.test.js (9 tests)`.

## Production pass

1-8, 10 n/a — documentation, no code: no input boundary, no error path, no
resource, no secret, no migration, no concurrency, no dependency.
9, the public surface is honest — this is the item that applies, and it is the
whole unit. The doc *was* dishonest in three places: rule 2 banned something the
browser permits, the enforcement paragraph claimed all six worked examples were
the gate's fixture when one is carried inverted, and the "Verifying a string"
lead-in implied the gate covers rule 2's trailing half when it covers it only
from two neutrals up and over-fires there. All three now say what is true, and
the over-strictness is named rather than hidden — including the direction of the
error, so a reader knows the cost is a reworded sentence and never a garbled
screen.

## Notes for the next unit

- The gate's own comment at `src/meta/bidi.test.js:70-76` still carries the claim
  R9 measured and found false: *"a sentence period plus its space lands on the
  far side of the Latin token it was meant to follow."* The doc no longer repeats
  it, but the code comment does. Fixing it means touching a file no unit has in
  `Files`, and it travels naturally with the deferred `brokenRuns` change (last
  bullet of the plan's `## Deferred`) — if the owner takes that decision, correct
  the comment in the same change.
- Nothing else is open. The false-positive class was already deferred before this
  unit ran; R10 describes it, as instructed, and does not re-raise it.
