# U1 — Gate the merge on a green test suite

status: done
date: 2026-08-18
branch: governance-ci-four-gates
commit: 11ef1ad
verified-on: pull request #20, runs 32140317879 (green), 32141231175 (red), 32141419201 (green again)

## What changed

A pull request against this repository now runs the vitest suite and reports a
check. Before this, CI ran only `npm ci` + `npm run build`, and only after a
merge landed on `master` — a red suite could reach production unnoticed.
`deploy.yml` is untouched, so the Pages deploy keeps its own concurrency group
and write permissions.

## Files

- `.github/workflows/ci.yml` — new; `pull_request` + `push` on `master`, runs `npm ci` then `npm run test`.

## Evidence

### Red observed

The unit's behaviour is "a failing suite turns the check red", so the red was
produced by appending a deliberately failing test to `src/utils/rows.test.js`
and running the exact command the workflow runs:

```
 FAIL  src/utils/rows.test.js > U1 deliberate red probe (temporary) > fails on purpose to prove the gate is not vacuous
AssertionError: expected 1 to be 2 // Object.is equality
 ❯ src/utils/rows.test.js:280:15

 Test Files  1 failed | 5 passed (6)
      Tests  1 failed | 973 passed (974)
EXIT_CODE=1
```

`npm run test` exits 1, which is what GitHub reads to mark the step failed. The
probe was reverted with `git checkout -- src/utils/rows.test.js`; the suite is
back to 973 passed / 6 files.

### Acceptance criteria

- [x] `ci.yml` runs on `pull_request` and on `push` to `master` — checked by reading `.github/workflows/ci.yml` → the `on:` block is `pull_request:` (all branches) and `push: branches: [master]`. No YAML parser is installed in this repo and `actionlint` is not on PATH, so validity was established structurally instead: `diff` of the `steps:` block against `deploy.yml` (a workflow GitHub already accepts) shows the checkout/setup-node/`npm ci` lines byte-identical, with only the trailing `- run:` line differing (`npm run build` → `npm run test`).
- [x] `deploy.yml` is unchanged — checked by `git diff master -- .github/workflows/deploy.yml` → empty output.
- [x] The job fails when and only when `npm run test` fails — checked by running `npm run test` in both states. Green: 973 passed, exit 0. With one broken assertion: 1 failed, exit 1, and the output names `src/utils/rows.test.js`. The step carries no `continue-on-error`, so that exit code is the step's verdict.

### Verification

"Opening a pull request produces a check whose log ends in vitest's pass
summary; the same pull request with a broken test shows the check failed."

**Confirmed on real pull-request CI — both halves.** Originally recorded as the
local half only; the remaining half was observed on 2026-08-18 by driving pull
request #20 through a green → red → green cycle.

| sha | run | check `test` |
|---|---|---|
| `fd74695` (U3 head, suite green) | `32140317879` | success |
| `befdac8` (deliberate breaks) | `32141231175` | **failure** |
| `98101af` (revert of `befdac8`) | `32141419201` | success |

The red run carried two independent breaks so a single run would settle both
this unit and U2: a failing assertion in `src/utils/ci-red-probe.test.js`, and
a temporary `| V20 |` row in `SPEC.md` §V with no matching test. The check log
names both:

```
FAIL src/meta/traceability.test.js > SPEC §V invariants are anchored to tests > every invariant has at least one test named after it
AssertionError: SPEC §V ids with no `V<n>:` test — tag a test, or write one: expected [ 'V20' ] to deeply equal []
FAIL src/utils/ci-red-probe.test.js > CI red probe > fails on purpose so the pull-request check can be watched going red
 Test Files  2 failed | 7 passed (9)
      Tests  3 failed | 987 passed (990)
```

`gh pr checks 20` reported `test  fail  9s` for that run. Both breaks were
reverted in `98101af`, which returned the check to success on the same pull
request — so the gate is neither vacuous (it goes red) nor stuck (it comes back
green).

### Full suite

`npm run test` → 6 files, 973 tests, all passed (4.12s).

## Production pass

1. **Failure paths** — the only failure modes are a non-zero exit from `npm ci` or `npm run test`; both propagate to the step and fail the check. No `continue-on-error`, no `|| true`. Observed exit 1 above.
2. **Untrusted input at the boundary** — the trigger is `pull_request`, not `pull_request_target`. A fork's pull request therefore runs against the merge ref with a read-only, fork-scoped token and cannot read repository secrets. This is the security-relevant choice in the file.
3. **Secrets** — `permissions: contents: read` only. No `secrets.*` is referenced and nothing is echoed. The job publishes nothing.
4. **Resource release** — n/a; an ephemeral GitHub-hosted runner is torn down by the platform.
5. **Timeouts / retries** — no `timeout-minutes` is set, so GitHub's default 360-minute job timeout applies. The suite runs in ~4.5s locally, so the exposure is a hung vitest burning runner minutes. Left at the default rather than added, because the Approach did not call for it; flagged here as a one-line change if it ever bites.
6. **Real size** — 973 tests / 6 files / ~4.5s. `npm ci` with the npm cache is the dominant cost. Nothing here grows with data.
7. **Observability** — vitest names the failing file, test and line in the check log (see the red above). That is the whole ask for this unit.
8. **Reversible** — deleting the file reverses it completely. Important caveat: this workflow only *blocks* a merge if branch protection requires the check. Whether protection is on for `master` is an open item in the plan's `## Deferred` and could not be read this session.
9. **Public surface** — a new check named `test` now appears on every pull request and on pushes to `master`. Contributors will see it; if branch protection is later configured, it is the check to require. Named in the commit message.

## Notes for the next unit

- The suite baseline the plan quotes is exact and current: 6 files, 973 tests, ~4.5s. U2/U3/U4 each add a file under `src/meta/`, which vitest picks up automatically — `vite.config.js` sets no `test.include`, so the default `**/*.{test,spec}.?(c|m)[jt]s?(x)` glob applies and a new `src/meta/*.test.js` needs no config change.
- Vitest 1.6.1, default `node` environment — `fs` and `path` work inside a test, which is what U2/U3/U4 all depend on. Confirmed by the version banner in the run output.
- No YAML parser and no `actionlint` are available in this environment (`node_modules` has neither `yaml` nor `js-yaml`; `actionlint` is not on PATH). `gh` is installed at `/c/Program Files/GitHub CLI/gh` but was not used to write anything.
- `.evidence/` and `CLAUDE.md` are untracked and pre-date this run. They were left unstaged deliberately; do not sweep them into a unit commit.
- The branch `governance-ci-four-gates` is shared by the whole run — U2/U3/U4 commit onto it, they do not branch again.
